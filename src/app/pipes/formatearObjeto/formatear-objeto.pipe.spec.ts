import { FormatearObjetoPipe } from './formatear-objeto.pipe';

describe('FormatearObjetoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearObjetoPipe();
    expect(pipe).toBeTruthy();
  });
});
