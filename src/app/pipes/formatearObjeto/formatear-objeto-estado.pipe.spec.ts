import { FormatearObjetoEstadoPipe } from './formatear-objeto-estado.pipe';

describe('FormatearObjetoEstadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearObjetoEstadoPipe();
    expect(pipe).toBeTruthy();
  });
});
