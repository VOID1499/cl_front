import { RestarHorarioPipe } from './restar-horario.pipe';

describe('RestarHorarioPipe', () => {
  it('create an instance', () => {
    const pipe = new RestarHorarioPipe();
    expect(pipe).toBeTruthy();
  });
});
